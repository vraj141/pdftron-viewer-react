import { down as composeDown } from 'docker-compose';

module.exports = async () => {
  try {
    await composeDown();
    console.log('Local Database Docker Container Stoped.');
  } catch (error) {
    console.log('Error on stop docker container:', error.message);
  }
};
